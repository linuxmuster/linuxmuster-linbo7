#!/bin/bash
#
# thomas@linuxmuster.net
# 20231114
#

# read build environment
for i in build/conf.d/*; do source "$i" || exit 1; done

# final depmod
depmod -a -b "$KSTBDIR" "$KVERS" || exit 1
depmod -a -b "$KLGCDIR" "$KVERS_LGC" || exit 1
depmod -a -b "$KLTSDIR" "$KVERS_LTS" || exit 1
